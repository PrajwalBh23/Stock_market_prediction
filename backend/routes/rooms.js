// import express from 'express';
// import { getAllRooms, getAllHostels, ViewMore, Searching, addRooms, myProperty, delectRoom, Saved, Showing, Removing } from '../controllers/controller.js';
// import { isAuthenticated } from '../controllers/auth.js';

// const router = express.Router();

// router.get("/rooms", getAllRooms);
// router.get("/hostels", getAllHostels);
// router.get('/search', Searching);
// router.get('/detail/:id', isAuthenticated, ViewMore);
// router.post("/add",isAuthenticated, addRooms);
// router.get("/property", isAuthenticated, myProperty);
// router.delete("/delete/:id", isAuthenticated, delectRoom)
// router.post('/saved/:roomId', isAuthenticated, Saved)
// router.get('/saved', isAuthenticated, Showing)
// router.delete('/remove/:roomId', isAuthenticated, Removing)

// export default router;